
		
					<% if user.image.attached? %>
  					<%=  link_to image_tag(user.display_image(size)), user  %>
					<% else %>
						<%= link_to gravatar_for(user, size: size), user %>
					<% end %>		

				<%= link_to user.name, user %>
					
		
