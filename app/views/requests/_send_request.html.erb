
	


<%= form_with(model: current_user.requests.build, local: true) do |f| %>
	<div><%= hidden_field_tag :pending_friend, user.id %></div>
	<%= f.submit "Send Friend Request", class: "btn btn-primary" %>
<% end %>	
